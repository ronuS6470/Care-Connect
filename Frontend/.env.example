# In dev, Vite proxies /api to the CareConnect API (see vite.config.ts) so no CORS policy is
# needed on the backend. Point this at the real API host directly for production builds.
VITE_API_BASE_URL=/api
